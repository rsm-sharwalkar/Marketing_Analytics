---
title: "Analysis of Student Performance"
author: "Shalini Harwalkar"
date: "April 2026"
format: html
---

## Introduction

This analysis explores how study habits and attendance influence student performance. Understanding these relationships can help identify key factors that drive academic success.

---

## Study Hours vs Performance

The scatter plot below shows the relationship between study hours and final exam scores.

```{python}
import numpy as np
import pandas as pd
import matplotlib.pyplot as plt

np.random.seed(42)

n = 150
study_hours = np.random.randint(1, 10, n)
attendance = np.random.randint(50, 100, n)

score = 5 * study_hours + 0.5 * attendance + np.random.normal(0, 5, n)

df = pd.DataFrame({
    "study_hours": study_hours,
    "attendance": attendance,
    "score": score
})

plt.figure()
plt.scatter(df["study_hours"], df["score"])
plt.xlabel("Study Hours")
plt.ylabel("Final Score")
plt.title("Study Hours vs Performance")
plt.show()
```
